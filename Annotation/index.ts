//1
let username: string = "Donuzz";
let score: number = 89.5;
let isLoggedIn: boolean = true;

//2
function greet(name: string): string {
  return `Hello, ${name}!`;
}

console.log(greet("Donuzz"));

//3
function square(n: number): number {
  return n * n;
}

console.log(5);

//4
let age: number = 25;
age = 30;

function showStatus(status: boolean): boolean {
  return status;
}

//5
const message = function (message: string): string {
  return message;
};
